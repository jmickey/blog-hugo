# About Widget config
[recent_posts]
  title = ""                            # default: "Recent Posts"
  length = 4                            # default: 5
  excludeTypes = ["page"]               # default: ["page"]
